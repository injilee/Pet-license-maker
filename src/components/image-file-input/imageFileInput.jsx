import React from 'react';
import styles from '../../styles/imageFileInput.module.css';

const ImageFileInput = (props) => {
  return <button className={styles.imageBtn}>Image</button>;
};

export default ImageFileInput;
